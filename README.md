# Tutoring

Tutoring materials for COMP9417.

Just use `make` to make the slides.

For more pointed compilation:
- Make slides using `make slides`.
- Make notes using `make notes`. This isn't used often.
